

let iniitialState = {

};


const expensesReducer = (state = iniitialState, action) => {
    switch(action.type) {
        case 'qwe': {
            return
        }




        default: 
            return state;
    }
}


export default expensesReducer;